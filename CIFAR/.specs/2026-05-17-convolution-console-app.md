# Консольное приложение convolution

- **Дата**: 2026-05-17
- **Статус**: Approved
- **Связанные issue/PR**: (нет)

## Цели

Реализовать консольное приложение `convolution`, принимающее один аргумент — путь к конфигурационному файлу, которое:

1. Загружает набор однотипных изображений из сырого бинарного файла в RAM.
2. Загружает один или несколько наборов фильтров в формате PCFilters (`LoadPCFilters`).
3. Для каждого изображения и каждого фильтра строит две неотрицательные карты свёртки (+фильтр и −фильтр, ReLU).
4. Для каждого фильтра вычисляет `Vmax` в режиме `def_sparsity` или `def_saturation` и проецирует значения свёрток в целые спайки `[0, 10]`.
5. Записывает бинарный файл значений свёрток, CSV со спайками и текстовый лог с метриками по фильтрам.

## Не цели

- Обучение или генерация фильтров (`PCFilters`, `pcfilCIFAR`).
- Потоковая обработка изображений, не помещающихся в RAM.
- GUI, сеть, интеграция с симулятором SNN (только подготовка данных).
- Поддержка форматов фильтров, отличных от PCFilters v1.
- Изменение формата файлов PCFilters.
- Пространственный tiling одного изображения на несколько GPU и multi-GPU вообще (возможно при переходе к большим кадрам).
- Зависимость от OpenCV CUDA / cuDNN (только собственные CUDA-ядра).

## Функциональные требования

### Запуск

1. Синтаксис: `convolution <config.toml>` (единственный обязательный аргумент — путь к конфигу).
2. При отсутствии аргумента, ошибке чтения/разбора конфига или валидации — ненулевой код возврата и сообщение в `stderr`.

### Конфигурационный файл

Формат: **TOML 1.0** (текстовый, UTF-8, комментарии `#`, вложенные таблицы, массивы). Выбран за читаемость, явную типизацию и удобное задание списка «файл фильтров + stride» без дублирования ключей.

Пример:

```toml
[input]
file = "Workplace/CIFAR10.bin"
width = 32
height = 32

[[filters]]
file = "Workplace/PCFilters_3x3.txt"
stride = 1

[[filters]]
file = "Workplace/PCFilters_6x6.txt"
stride = 2

[projection]
mode = "def_sparsity"   # или "def_saturation"
criterion = 2.0         # смысл зависит от mode (см. ниже)

[compute]
use_gpu = false         # true — считать свёртки на GPU (CUDA)
device = 0              # номер одной CUDA-карты; игнорируется при use_gpu = false

[output]
convolutions_bin = "Workplace/conv_values.bin"
spikes_csv = "Workplace/conv_spikes.csv"
log = "Workplace/convolution.log"
```

Обязательные поля:

| Поле | Соответствие п. ТЗ |
|------|-------------------|
| `input.file` | 1. Имя входного файла |
| `input.width` | 2. Ширина |
| `input.height` | 3. Высота |
| `filters[].file` | 4. Файлы фильтров (PCFilters) |
| `filters[].stride` | 5. Stride для каждого файла |
| `projection.mode` | 6. `def_sparsity` \| `def_saturation` |
| `projection.criterion` | 7. Критерий (`def_sparsity`: `0…10`; `def_saturation`: доля `0…1`) |
| `output.convolutions_bin` | 8. Бинарный выход |
| `output.spikes_csv` | 9. CSV спайков |
| `output.log` | 10. Лог |

Опциональные поля compute (значения по умолчанию):

| Поле | По умолчанию | Назначение |
|------|--------------|------------|
| `compute.use_gpu` | `false` | Включить расчёт свёрток на GPU |
| `compute.device` | `0` при `use_gpu = true` | Номер одной CUDA-карты (ordinal) |

### Входные изображения

1. Формат как в `pcfilCIFAR` / `unsfil/unsfilc.cpp`: подряд без заголовка, каждое изображение — `width × height × channels` байт `uint8`, порядок каналов **interleaved** (как OpenCV `CV_8UCn`, в т.ч. `CV_8UC3` для CIFAR10.bin проекта).
2. **`channels` не задаётся в конфиге** — берётся из метаданных PCFilters (`channels=` в каждом `[Filter*]`). Перед чтением изображений загружаются (или просматриваются) файлы фильтров; во всех `[[filters]]` значение `channels` должно совпадать, иначе ошибка.
3. Число изображений: `file_size / (width * height * channels)`; остаток от деления — ошибка.
4. Все изображения одного размера; тип матрицы `CV_8UC(channels)`.

### Валидация конфига

- `projection.mode` — ровно `def_sparsity` или `def_saturation`.
- `projection.criterion` при `def_sparsity`: `0 ≤ criterion ≤ 10`.
- `projection.criterion` при `def_saturation`: `0 < criterion ≤ 1` (доля).
- `filters` — непустой массив; `stride ≥ 1` для каждой записи.
- `compute.use_gpu` — булево; при отсутствии — `false`.
- При `use_gpu = true`: `device` — целое `≥ 0`, устройство должно существовать (иначе фатальная ошибка).
- При `use_gpu = false`: поле `device` игнорируется (можно не указывать).
- При `use_gpu = true` и отсутствии `device` — использовать `0`.

### Вычисления на CPU / GPU

1. При `use_gpu = false` — свёртки на CPU (эталонная реализация).
2. При `use_gpu = true` — свёртки (+/−, ReLU) выполняются на **одной** GPU через **собственные CUDA-ядра** (не OpenCV CUDA, не cuDNN). Подбор `Vmax`, проекция в спайки и запись файлов остаются на CPU.
3. Multi-GPU и пространственный tiling одного кадра — **вне объёма** текущей версии; при необходимости (большие изображения) расширить отдельно.
4. Если `use_gpu = true`, а CUDA недоступна при сборке или runtime / номер устройства невалиден — фатальная ошибка (не молчащий fallback на CPU).
5. Числовые результаты GPU и CPU должны совпадать с точностью, достаточной для тех же целых спайков на типичных данных; допустимы мелкие float-расхождения до проекции (зафиксировать в тестах допуск или сравнение CSV).

### Фильтры и свёртка

1. Загрузка через существующую `LoadPCFilters(std::vector<cv::Mat> &, const char *)`.
2. Для каждой записи `[[filters]]` все фильтры из файла используют один `stride`.
3. Размер выхода для изображения `I` и фильтра `F` (как в `matConvolution` в `UnsupervisedFiltersMultiple.cpp`):

   `out_w = (I.cols - F.cols) / stride + 1`  
   `out_h = (I.rows - F.rows) / stride + 1`

   Целочисленное деление; если `out_w < 1` или `out_h < 1` — ошибка (фильтр не помещается).

4. Значение свёртки в позиции `(x, y)`: скалярное произведение патча изображения и фильтра (`cv::Mat::dot` на ROI, фильтр и патч приводятся к `CV_32F` при необходимости).
5. Для каждого фильтра `k` — **две** карты:
   - `conv_plus[k]` = `max(conv(I, +F_k), 0)`
   - `conv_minus[k]` = `max(conv(I, −F_k), 0)` (эквивалентно `max(−conv(I, +F_k), 0)` при той же формуле dot).
6. Порядок обхода для записи в выходные файлы (зафиксировать в реализации и в логе):
   - по индексу изображения (0 … N−1);
   - по порядку секций `[[filters]]` в конфиге;
   - по индексу фильтра внутри файла (0 … count−1);
   - полярность: `plus`, затем `minus`;
   - внутри карты: строка `y` от 0 до `out_h−1`, столбец `x` от 0 до `out_w−1`.

### Проекция в спайки

1. Период предъявления изображения фиксирован: **10** (используется только при интерпретации счётчиков спайков в CSV; на формулу `Vmax` не влияет).
2. Для каждого **физического фильтра** (одна матрица из PCFilters) один `Vmax > 0`, общий для обеих полярностей и всех изображений.
3. Проекция значения `v ≥ 0`:

   - если `v ≤ Vmax`: `spike = round(10 * v / Vmax)` с округлением к **ближайшему** целому (половинки — к чётному, banker's rounding как в C++ `std::round` / уточнить в реализации);
   - если `v > Vmax`: `spike = 10`.

   Итог: `spike ∈ {0, …, 10}`.

4. **def_sparsity**: подобрать `Vmax` так, чтобы **среднее** число спайков по **всем** значениям свёрток (обе полярности, все позиции, все изображения) для данного фильтра было **максимально близко** к `projection.criterion`. Допустимый диапазон критерия: **`0 ≤ criterion ≤ 10`** (среднее число спайков).
5. **def_saturation**: подобрать `Vmax` так, чтобы доля значений свёртки **строго больше** `Vmax` среди того же полного множества была **максимально близка** к `projection.criterion`. Критерий — **доля в интервале `(0, 1]`**, не проценты (например `0.05` означает 5%).
6. Алгоритм подбора `Vmax`: двухпроходный расчёт — накопить все `v`, затем **перебор только по уникальным значениям** из выборки (непрерывная оптимизация не требуется).
7. Если для фильтра **все** значения свёртки равны нулю (`max(V) = 0`) — **фатальная ошибка**: сообщение в `stderr`, ненулевой код возврата, **выходные файлы не создавать и не изменять**.

### Лог

Для каждого фильтра (с указанием файла и индекса в файле) записать строку:

| Режим | Поля в логе |
|-------|-------------|
| `def_sparsity` | `Vmax`, доля значений свёртки `> Vmax` |
| `def_saturation` | `Vmax`, достигнутое среднее число спайков по всем свёрткам с этим фильтром |

Дополнительно в начале лога: версия приложения, путь к конфигу, число изображений, режим, критерий, размеры карт, `use_gpu`, устройство (`device=N` или `cpu`).

### Выходные файлы

**Бинарный (`output.convolutions_bin`):**

- Все скалярные значения свёртки **после ReLU**, **до** проекции в спайки.
- Тип: `float64`, little-endian (native на x86_64).
- Порядок байт — как в п. «Порядок обхода».

**CSV (`output.spikes_csv`):**

- **Заголовка нет.**
- **Одна строка файла — одно изображение** (ровно столько строк, сколько изображений).
- В строке — все спайки `0`…`10` для этого изображения в порядке обхода из п. «Порядок обхода» (все `[[filters]]`, все фильтры, `plus`/`minus`, все позиции), разделитель полей — **запятая** (`,`), без пробелов (допустимы пробелы после запятой при чтении не требуются).
- Порядок значений в строке изображения `i` совпадает с подпоследовательностью бинарного файла, относящейся к изображению `i` (bin — плоская последовательность `float64`, CSV — та же последовательность, сгруппированная по строкам).

## Нефункциональные требования

- **Производительность**: полный проход по датасету в RAM; для CIFAR-10 (50k×32×32×3) и ~20 фильтров 3×3 целевое время — минуты на desktop CPU; при `use_gpu = true` — существенно быстрее на сопоставимой GPU.
- **Память**: хранить все изображения и все карты свёрток в host RAM допустимо (явное требование ТЗ); на GPU — батчинг/streaming, чтобы не требовать размещения всего датасета в VRAM сразу.
- **Потокобезопасность**: однопоточный host; один CUDA-контекст на выбранном `device`.
- **Совместимость**: C++17, OpenCV Core (host), CUDA Toolkit + собственные `.cu` ядра; Windows (MSVC) и Linux (GCC/Clang). Сборка без CUDA должна давать рабочий CPU-режим; GPU — при наличии toolchain.
- **Наблюдаемость**: основной отчёт — `output.log`; прогресс (опционально) — `stderr` при длительных прогонах.
- **Зависимости конфига**: парсер TOML (header-only, например [toml++](https://github.com/marzer/tomlplusplus) MIT); для GPU — CUDA Toolkit (без обязательной зависимости от OpenCV CUDA / cuDNN).

## Принятые решения (закрытые вопросы)

| # | Решение |
|---|---------|
| 1 | Число каналов входа — из PCFilters (`channels` в файле фильтров), не из конфига. |
| 2 | Отдельное согласование каналов изображения/фильтра не требуется: каналы определяются фильтрами, изображение читается с тем же `channels`. |
| 3 | `def_saturation`: `criterion` — доля `(0, 1]`, не проценты. |
| 4 | `def_sparsity`: `0 ≤ criterion ≤ 10`. |
| 5 | Все свёртки фильтра нулевые — ошибка, выход без записи результатов. |
| 6 | CSV: без заголовка; одна строка на изображение; значения через `,`. |
| 7 | `Vmax` — перебор по уникальным значениям выборки. |
| 8 | Проект `convolution` — CMake + **`convolution.vcxproj` в `CIFAR.sln`** (зависимость от `PCFilters`). |
| 9 | GPU: в конфиге `compute.use_gpu` и `compute.device` (одна CUDA-карта); при `use_gpu = false` — только CPU. |
| 10 | GPU-стек: **собственные CUDA-ядра** (не OpenCV CUDA, не cuDNN). |
| 11 | Только **один** GPU в текущей версии; multi-GPU / tiling больших кадров — отложено. |

## Предполагаемые изменения в коде

### Новые классы / типы

- `ConvolutionConfig` — структура с полями конфига после разбора TOML (включая `use_gpu`, `device`); валидация путей и диапазонов.
- `ConvolutionConfigParser` — чтение TOML → `ConvolutionConfig` (можно объединить с `ConvolutionConfig` как свободные функции в `ConvolutionConfig.cpp`).
- `RawImageLoader` — чтение бинарного потока в `std::vector<cv::Mat>`.
- `FilterBank` — один загруженный PCFilters-файл + `stride`.
- `ConvolutionRunner` — свёртка +/-, ReLU, накопление значений для `Vmax` (CPU).
- `GpuConvolutionRunner` — GPU-путь на собственных CUDA-ядрах (те же семантика и порядок выхода); при `use_gpu = false` не используется.
- `VmaxEstimator` — режимы `def_sparsity` / `def_saturation`.
- `SpikeProjector` — линейная проекция в `[0, 10]`.
- `ConvolutionOutputWriter` — бинарный поток, CSV, лог.

(Допустима реализация без классов — только namespace + free functions; имена оставить как ориентир для модулей.)

### Изменённые классы

- Нет.

### Удалённые классы

- Нет.

### Прочие артефакты

- **Новый каталог**: `convolution/` (`convolution.cpp`, заголовки, `CMakeLists.txt`, `convolution.vcxproj`, `convolution.vcxproj.filters`).
- **Изменён**: корневой `CMakeLists.txt` — `add_subdirectory(convolution)`.
- **Изменён**: `CIFAR.sln` — проект `convolution` с зависимостью от `PCFilters` (по аналогии с `pcfilCIFAR`).
- **Используются без изменения API**: `LoadPCFilters` (`PCFilters/PCFilters.h`).
- **Референс для свёртки**: `matConvolution` в `UnsupervisedFiltersMultiple.cpp` (логика размера и stride; не линковать модуль, если не нужен — скопировать формулу или вынести общую утилиту в отдельном PR).

## Библиотеки и зависимости

- **Новые**: toml++ (парсинг конфига), header-only, MIT.
- **Существующие**: OpenCV Core (`cv::Mat`, dot product), статическая библиотека `PCFilters`.
- **GPU (опционально при сборке)**: CUDA Toolkit + собственные `.cu` ядра. OpenCV CUDA / cuDNN **не** требуются.
- **Toolchain**: CMake ≥ 3.14, C++17; для GPU-сборки — NVCC / CUDA-capable MSVC toolchain.

## Критерии приёмки

### Автотесты

- [ ] Юнит-тест: разбор минимального валидного TOML → ожидаемый `ConvolutionConfig` (в т.ч. `use_gpu` / `device` и defaults).
- [ ] Юнит-тест: размер карты свёртки `(32, 3×3, stride=1) → 30×30`, `(32, 6×6, stride=2) → 14×14`.
- [ ] Юнит-тест: ReLU и вторая полярность (`conv_minus` = ReLU(−conv_raw)).
- [ ] Юнит-тест: `SpikeProjector` — границы `0`, `Vmax`, `> Vmax → 10`, округление.
- [ ] Юнит-тест: `VmaxEstimator` на синтетическом массиве — `def_sparsity` даёт среднее ≈ criterion; `def_saturation` — доля выше порога ≈ criterion.
- [ ] Интеграционный тест: 2–3 искусственных изображения 8×8, 1–2 фильтра 3×3 из тестового PCFilters-файла — сравнение bin/CSV с эталоном (golden files); CSV: число строк = числу изображений.
- [ ] Ошибка при `max(V)=0` для фильтра — exit ≠ 0, выходные файлы отсутствуют.
- [ ] Smoke: запуск на `Workplace/CIFAR10.bin` + `Workplace/PCFilters_3x3.txt`, оба режима, ненулевой exit code только при ошибке.
- [ ] GPU (если есть CUDA): тот же маленький набор — CSV совпадает с CPU-эталоном; неверный `device` / `use_gpu` без CUDA → ошибка.

### Линтеры и качество

- [ ] Сборка Debug и Release (Windows MSVC через `CIFAR.sln` / Linux GCC через CMake).
- [ ] Стиль и предупреждения — в духе существующих таргетов (`-fpermissive` где уже принято).
- [ ] `clang-format` по профилю репозитория (если появится `.clang-format` — применить к новым файлам).

## План внедрения (опционально)

1. **PR1**: каркас `convolution`, TOML-конфиг (включая `compute`), загрузка изображений и PCFilters, свёртка +/- на CPU без `Vmax`.
2. **PR2**: `VmaxEstimator`, проекция, запись bin/CSV/log, smoke на Workplace.
3. **PR3**: GPU-путь (`use_gpu`, `device`), сравнение с CPU на маленьком наборе.
4. **PR4** (опционально / позже): тесты; multi-GPU или tiling для больших кадров — отдельная задача.

## Приложение: семантика режимов (формулы)

Пусть для фильтра `k` множество всех неотрицательных значений свёртки  
`V = { v_ij }` (обе полярности, все изображения и позиции), `N = |V|`.

**Проекция** при фиксированном `Vmax > 0`:

\[
\text{spike}(v) = \begin{cases}
10 & v > Vmax \\
\mathrm{round}(10 \cdot v / Vmax) & 0 \le v \le Vmax
\end{cases}
\]

**def_sparsity** — найти `Vmax`, минимизирующее `|mean_{v \in V} spike(v) - criterion|`.

**def_saturation** — найти `Vmax`, минимизирующее `|\#\{v \in V : v > Vmax\} / N - criterion|`.

**Лог:**

- `def_sparsity`: вывести найденный `Vmax` и `#{v > Vmax}/N`.
- `def_saturation`: вывести `Vmax` и `mean_{v \in V} spike(v)`.
